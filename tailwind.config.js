// tailwind.config.js
theme: {
  extend: {
    colors: {
      albion: {
        dark: '#1a140f', // Fondo profundo (noche medieval)
        panel: '#2d221a', // Fondo de contenedores (madera oscura)
        border: '#5c4433', // Bordes de hierro/madera
        accent: '#d4af37', // Dorado de ítems legendarios
        paper: '#e8d4b4', // Color de pergamino para textos
      }
    }
  }
}
